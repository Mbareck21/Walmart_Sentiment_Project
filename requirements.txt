# VoC Insight Engine — runtime dependencies (all free / open-source)
streamlit>=1.40
pandas>=2.2
nltk>=3.9
plotly>=5.24
openai>=1.54          # OpenAI-compatible client — drives the FREE Ollama backend by default
python-dotenv>=1.0    # loads local .env for backend config
